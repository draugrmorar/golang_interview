Шардирование (от англ. shard — осколок) — это метод горизонтального масштабирования баз данных,
при котором большая таблица физически разбивается на несколько независимых частей (шардов)
и распределяется по разным серверам